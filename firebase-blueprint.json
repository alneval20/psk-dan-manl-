{
  "entities": {
    "Service": {
      "title": "Service",
      "description": "A psychological counseling service offered by the consultant.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the service in Turkish."
        },
        "title_en": {
          "type": "string",
          "description": "The title of the service in English."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the service in Turkish."
        },
        "description_en": {
          "type": "string",
          "description": "A brief description of the service in English."
        },
        "icon": {
          "type": "string",
          "description": "The name of the Lucide icon to use."
        },
        "order": {
          "type": "number",
          "description": "The display order of the service."
        }
      },
      "required": ["title", "description", "icon", "order"]
    },
    "SiteSettings": {
      "title": "Site Settings",
      "description": "Global configuration and content for the website.",
      "type": "object",
      "properties": {
        "heroTitle": { "type": "string" },
        "heroTitle_en": { "type": "string" },
        "heroSubtitle": { "type": "string" },
        "heroSubtitle_en": { "type": "string" },
        "aboutText": { "type": "string" },
        "aboutText_en": { "type": "string" },
        "aboutImage": { "type": "string" },
        "email": { "type": "string" },
        "instagram": { "type": "string" },
        "consultantName": { "type": "string" },
        "consultantTitle": { "type": "string" },
        "consultantTitle_en": { "type": "string" }
      },
      "required": ["heroTitle", "heroSubtitle", "aboutText", "email", "instagram", "consultantName"]
    },
    "Appointment": {
      "title": "Appointment Request",
      "description": "A request for a counseling session submitted by a potential client.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "phone": { "type": "string" },
        "service": { "type": "string", "enum": ["Çocuk Danışmanlığı", "Ergen Danışmanlığı", "Yetişkin Danışmanlığı", "Diğer"] },
        "message": { "type": "string" },
        "preferredDate": { "type": "string" },
        "status": { "type": "string", "enum": ["Bekliyor", "İletişime Geçildi", "Tamamlandı"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "email", "phone", "service", "status", "createdAt"]
    },
    "Availability": {
      "title": "Availability",
      "description": "Available time slots for a specific date.",
      "type": "object",
      "properties": {
        "date": { "type": "string", "description": "Date in YYYY-MM-DD format." },
        "slots": { 
          "type": "array", 
          "items": { "type": "string" },
          "description": "List of available time slots (e.g., ['09:00', '10:00'])."
        }
      },
      "required": ["date", "slots"]
    }
  },
  "firestore": {
    "services": {
      "schema": "Service",
      "description": "Collection of counseling services."
    },
    "settings": {
      "schema": "SiteSettings",
      "description": "Global site settings (singleton collection)."
    },
    "appointments": {
      "schema": "Appointment",
      "description": "Collection of appointment requests."
    },
    "availability": {
      "schema": "Availability",
      "description": "Collection of available time slots for appointments."
    }
  }
}
